# 冯恩琪-25361023-第二次人工智能编程作业


## 1. 任务拆解与 AI 协作策略
（请简述你在编写代码前，是如何将这个大任务拆解给 AI 的？先让 AI 写了什么，后写了什么？） 
步骤1：我先让AI编写了实现读取文件并查询学号对应学生详细信息的功能（同时实现对输入的校验），在提示词中强调了编程规范（要求使用的类的封装方式和方法，以及只能使用标准库）和输出格式（给一个案例让AI模仿）；
步骤2：在AI生成代码后，对“提示输入错误语言不够友善”“使用了复杂的可读性低的名字”两个问题进行了提示修改；
步骤3：在调试完这一功能之后继续让AI实现随机抽取学生的功能（同时实现对输入的校验），再次在提示词中强调了编程规范和输出格式（依旧给一个案例让AI模仿）；
步骤4：在AI生成代码后，对“提示输入错误语言中出现emoji可能不兼容，需要删除emoji”的问题进行了提示修改；
步骤5：在调试完这一功能之后继续让AI实现“生成考场安排表的功能（任务较为复杂，将其拆解为以下几个步骤），再次在提示词中强调了编程规范和输出格式（依旧给一个案例让AI模仿）；
      拆解步骤（原文复制）：
      1.给每个学生随机分配考场座位号（1到N）；
      2.在程序根目录下输出一份“考场安排表.txt”的文件，文件的输出格式应参考“座位号 | 学生姓名 | 学号”的格式；
      文件的第一行必须包含该安排表的生成时间（格式如：生成时间：2026-03-23 10:00:00）。
步骤6：在AI生成代码后，对“输出没有按座位号升序输出，而只是在原有的输入顺序前添加座位号”的问题进行了提示修改；，要求AI（原文复制）
  在生成考场安排表的时候，要做的事情是：
  1）给每个学生安排不同的座位号；
  2）按随机安排的座位号从小到大的顺序（而不是依据原来的学号顺序打印结果）；
  3）在程序根目录下输出一份“考场安排表.txt”的文件，文件的输出格式应参考“座位号 | 学生姓名 | 学号”；
  4）文件的第一行必须包含该安排表的生成时间（格式如：生成时间：2026-03-23 10:00:00）。
步骤7：在调试完这一功能之后继续让AI实现生成准考证的功能，同时在文件命名要求中意识到输出的考场座位序号不够规范（并不都是两位数字），并要求AI更改“如果不满足两位，就使用0左补齐输出，如‘07’”，再次在提示词中强调了编程规范和输出格式；
步骤8：在AI生成代码后，对“功能2的非法输入校验功能代码被删除”的问题进行了提示修改；
步骤9：在AI提交代码后对个钟异常输入进行了测试，确定无误以后确定最终版本。


## 2. 核心 Prompt 迭代记录
（展示一次你通过修改提示词，让 AI 的代码从“不符合要求”变成“完美符合工程规范”的过程） 
此处展示上述步骤8的迭代记录
初代 Prompt：
请你在我的源代码的基础上进行修改并增加新功能“生成准考证目录与文件”：
修改：将每个人的考场安排的座位序号全部统一为两位数字的标准输出（如果不满足两位，就使用0左补齐输出，如“07”；
新增功能“生成准考证目录与文件”：
在根目录下创建一个名为“准考证”的文件夹。根据考场安排信息，在该文件夹内生成『01.txt』、『02.txt』等独立文件，每个文件内写入对应学生的考场座位号、姓名、学号信息。 
AI 生成的问题/缺陷：功能2的非法输入校验功能代码被删除
优化后的 Prompt (追问)：在实现功能2的时候，你丢失了我原本的校验功能，请你不要修改功能12的代码，仅修改功能3的代码并实现功能4“生成准考证目录与文件”


## 3. Debug 与异常处理记录
在与AI交互过程中并未遇到此类问题（真的没有遇到TNT）
（请贴出一段 AI 生成的核心逻辑代码，并加上你自己的逐行中文注释，证明你完全理解了它的运行机制）```python


# 贴入代码及人工注释
    def generate_exam_arrangement(self):
        """
        功能3：生成考场安排表。
        1. 随机分配座位。
        2. 座位号统一为两位数输出。
        3. 按座位号升序写入文件。（这部分是我给AI拆解的过程指令）
        """
        if not self.students_map:               #排除特殊情况（空）
            print("数据为空，无法生成考场安排表。")
            return

        #转换为列表进行随机分配数字（座位号）
        student_list = list(self.students_map.values())          #转换为列表
        num_students = len(student_list)                        #记录学生人数

        # 随机分配 1 到 N 的座位号
        seats = list(range(1, num_students + 1))
        random.shuffle(seats)                                   #用random.shuffle（）打乱里面的元素，实现给每个人分配随机的座位
        for i in range(num_students):
            student_list[i].seat_number = seats[i]              #分配好作为以后将座位号存进原本的数据集，这样保证座位号成为了学生的属性之一，方便后续功能使用

        student_list.sort(key=lambda x: x.seat_number)          #按照座位号从小到大排序（规范打印的安排表）

        file_name = "考场安排表.txt"
        create_time = self.get_current_time_str()

#这是一个保护机制，当磁盘满了、文件被占用或者没有写入权限时，保证程序不会直接崩溃，而是跳到下面的except处理错误。
        try:
            with open(file_name, "w", encoding="utf-8") as f:           #打开文件并写入，并保证文件可以自动关闭
                f.write(f"生成时间：{create_time}\n")                     #输出生成时间
                for s in student_list:
                    # 座位号格式化为两位数字
                    formatted_seat = f"{s.seat_number:0>2}"
                    f.write(f"{formatted_seat} | {s.name} | {s.student_id}\n")      #格式化输出的表格
            print(f"\n[成功] 考场安排表已生成至：{os.path.abspath(file_name)}")         #输出文件生成路径
        except Exception as e:
            print(f"文件写入失败：{e}")        #处理错误
